# task-014 — Scenario/corpus/registry/replay schemas

**Status:** DONE - REVIEW PASS

Define versioned scenario, corpus, registry and replay-trace schemas with closed enums, bounded arrays and canonical cross-object references. Reject malformed references and unapproved executable/network fields before evaluation.

## Evidence

`src/model.rs`, `src/schema.rs`, and four schemas under `schemas/mcp-auth-security/v1/` generated by `scripts/k18/gen_mcp_auth_schemas.py` with a `--check` mode. Generated rather than hand-written so the closed enums cannot drift between documents. The scenario type has no field in which it could state its own verdict.
