# Agentic demo (integrated workflow)

Exercises product assess with coverage, attack graph, and continuous plan-only validation.

```bash
cargo run -p dare-agent-security -- assess examples/agentic-demo --offline --confidential
cargo run -p dare-agent-security -- report --path examples/agentic-demo
```
