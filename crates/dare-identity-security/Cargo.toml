[package]
name = "dare-identity-security"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Bounded, evidence-first, offline-first identity, privilege and delegation security validation (Cycle 015)"
publish = false

[lib]
name = "dare_identity_security"
path = "src/lib.rs"

[dependencies]
dare-adversarial = { path = "../dare-adversarial" }
dare-coaz-integrity = { path = "../dare-coaz-integrity" }
dare-coverage = { path = "../dare-coverage" }
dare-security-evidence = { path = "../dare-security-evidence" }
jsonschema = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = "2.0"
time = { workspace = true }

[dev-dependencies]
tempfile = "3.14"
